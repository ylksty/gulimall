server:
  port: 7000

spring:
  application:
    name: server-coupon
  cloud:
    # nacos配置
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: MALL_GROUP
        namespace: 9ec92519-3da1-4a83-8ff0-c7c2a7fcb9a8

# ${prefix}-${spring.profiles.active}.${file-extension}
# server-coupon-dev.yaml
# server-coupon-test.yaml
# server-coupon-prod.yaml

coupon:
  user:
    name: joe
    age: 77