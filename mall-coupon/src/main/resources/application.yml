spring:
  profiles:
    active: dev # 表示开发环境
#    active: test # 表示测试环境
#    active: prod
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.33.10:3306/mall_sms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  #  mapper-locations: classpath*:/mapper/**/*.xml # 扫描所有包
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto